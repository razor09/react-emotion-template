import { observer } from 'mobx-react';
import { StartPageComponent } from './StartPage.d';

export const StartPage = observer<StartPageComponent>(() => {
  return <div>React Emotion Template</div>;
});
